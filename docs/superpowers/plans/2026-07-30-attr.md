# v349 — flip `attr` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Flip the bash-suite `attr` category to PASS by fixing `readonly`-builtin divergences.

**Architecture:** Roots A+C are in `builtin_readonly_decl` (`crates/huck-engine/src/builtins.rs` ~1816); Root D is in the executor's declaration-builtin arg construction (quoted `name=value` → `DeclArg::Assign`); Root B resolves once A lands.

**Tech Stack:** Rust (huck-engine crate), bash-diff harness.

**Spec:** `docs/superpowers/specs/2026-07-30-attr-design.md`. **Issue:** [#343](https://github.com/jdstanhope/huck/issues/343).

## Global Constraints

- Commit trailer on every commit: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- `cargo fmt --all` before every commit (CI enforces `--check`).
- Branch `v349-attr`; never push to main, never self-merge.
- Build the binary with `cargo build -p huck`. Per-crate lib tests: `cargo test -p huck-engine --lib --jobs 1 -- --test-threads 1`. NEVER `cargo test --workspace` (OOM on this 1-core/1.9GB box). Guard sweeps with `ulimit -v 1500000` + `timeout`.
- Bash reference: `bash --norc --noprofile`. `.right` is authoritative (local bash 5.2.21 == `attr.right`). ⚠️ Confirm any category-diff with `diff attr.right <huck-output>` — the raw runner diff reads inverted for this category.
- No behavior change beyond the four roots; `declare`/`export`/`local` must stay passing.

---

### Task 1: Root A (`readonly -a`) + Root C (error prefix)

**Files:**
- Modify: `crates/huck-engine/src/builtins.rs` — `builtin_readonly_decl` (~1816): the flag loop (add `-a`) and the `DeclArg::Assign` readonly-variable error.
- Test: engine readonly/declare tests.

**Interfaces:** the flag loop already has `want_associative` (`-A`) with a `shell.declare_associative(name)` guard; add the indexed analogue (huck's indexed-array declare — find it, e.g. `declare_indexed`/`ensure_indexed`, `grep -n 'fn declare_indexed\|fn ensure_indexed\|make_indexed' crates/huck-engine/src/shell_state.rs`).

- [ ] **Step 1: Reproduce + failing tests.**

```
readonly -a x=(1 2); declare -p x    # bash: declare -ar x=([0]="1" [1]="2")   huck: readonly: -a: invalid option
readonly x=1; readonly x=2           # bash: x: readonly variable   huck: readonly: x: readonly variable
```

Add tests. KEEP: `readonly -A m=([k]=v); declare -p m` still works; `readonly -q` still errors `readonly: -q: invalid option` (prefix kept); plain `x=1; readonly x; x=5` → `x: readonly variable` (already bare).

- [ ] **Step 2: Run, verify fail.**

- [ ] **Step 3: Implement.** Root A: add `"-a" => { want_indexed = true; idx += 1; }`; where `want_associative` ensures associativity, add the `want_indexed` branch ensuring the name is an indexed array before marking readonly / before `apply_one_assignment`. Root C: change the `DeclArg::Assign` error `readonly: {name}: readonly variable` → `{name}: readonly variable`. Verify Root A array-value (`readonly -a r=(7); declare -p r` → `declare -ar r=([0]="7")`) and that Root B's category residual is gone. Verify EACH FIX + KEEP vs bash.

- [ ] **Step 4: Run tests + hand-check.**

- [ ] **Step 5: Commit.** (`v349: readonly -a indexed-array attribute + bare readonly-variable error (#343)`, trailer.)

---

### Task 2: Root D — quoted `readonly 'name=(val)'` parsed as an assignment

**Files:**
- Modify: the executor's declaration-builtin arg construction (find where `readonly`/`declare`/`export` args become `DeclArg::Assign`/`Plain` — `grep -rn 'DeclArg::Assign\|DeclArg::Plain' crates/huck-engine/src/executor.rs`).
- Test: engine tests.

- [ ] **Step 1: Reproduce + failing test.**

```
readonly a; readonly 'a=(3)'    # bash: a: readonly variable   huck: readonly: `a=(3)': not a valid identifier
readonly '3x=1'                 # bash: `3x=1': not a valid identifier   (KEEP — invalid ident stays Plain)
```

Add a test. Also verify the quoted-value semantics: with a pre-existing array `c=(outside)`, `readonly 'c=(3)'` → bash `declare -ar c=([0]="(3)")` (quoted `(3)` is a literal scalar value into element 0).

- [ ] **Step 2: Run, verify fail.**

- [ ] **Step 3: Implement.** An arg of the form `<valid-ident>=<value>` reaching a declaration builtin must become `DeclArg::Assign` even when the word was quoted (currently a fully-quoted word → `Plain`). Guard: only when the part left of the first `=` is a valid identifier; otherwise stays `Plain`. Verify the value handling matches bash (a quoted `(3)` is a literal scalar, NOT an array literal). Verify no regression to `declare`/`export`/`local` (run those categories/tests).

- [ ] **Step 4: Run tests + hand-check.**

- [ ] **Step 5: Commit.** (trailer.)

---

### Task 3: `attr` diff-check harness + category-flip verification + docs/memory

**Files:**
- Create: `tests/scripts/attr_diff_check.sh`.
- Modify: `docs/bash-test-suite-baseline.md` (PASS 36 → 37); memory files.

- [ ] **Step 1: Write the harness.** `tests/scripts/attr_diff_check.sh` modeled on `appendop_diff_check.sh`. Cover: Root A (`readonly -a x=(1 2); declare -p x`; `readonly -a`+`-A`); Root C (`readonly x=1; readonly x=2`); Root D (`readonly a; readonly 'a=(3)'`); regressions (`readonly -A m=([k]=v)`; plain `readonly r=(7)`; `readonly -q` invalid-option keeps prefix; `readonly '3x=1'` invalid identifier). `chmod +x`.

- [ ] **Step 2: Harness green.** `cargo build -p huck`; `ulimit -v 1500000; timeout 120 bash tests/scripts/attr_diff_check.sh`.

- [ ] **Step 3: Official `attr` runner (flip signal).** `BASH_SOURCE_DIR=/tmp/bash-5.2.21 HUCK_BASH_TEST_CATEGORY=attr bash tests/bash-test-suite/runner.sh` → `attr | PASS`; also `diff attr.right <huck-output>` = 0 (direction-safe).

- [ ] **Step 4: No-regression sweep.** Build release + debug; `run_diff_checks.sh` green (incl. new harness). Full bash-suite runner: PASS-set == v348 baseline (36) **+ `attr`** = 37, nothing regressed — Root D touches the shared declaration-arg construction, so verify by diffing the PASS-set AND spot-check declare/export/local categories explicitly. Run the declare/readonly/export `-p huck` integration bins.

- [ ] **Step 5: Update docs + memory.** `docs/bash-test-suite-baseline.md`: v349 block, PASS 36→37, flip the `attr` row + PASS-list. Append v349 to `project_huck_iterations.md` + a hook to `MEMORY.md` (compact if >~17KB). Commit harness + baseline (memory files are outside the repo — update but don't `git add`).

---

## Self-Review

**Spec coverage:** Root A+C → Task 1; Root D → Task 2; Root B (resolves via A, verified) → Task 1 Step 3; harness + flip + no-regression + docs → Task 3. All covered.

**Placeholder scan:** the indexed-array declare helper (Task 1) and the DeclArg-construction site (Task 2) are `grep`-away lookups with concrete FIX/KEEP tables — bounded, not placeholders.

**Consistency:** `want_associative`/`-A` is the template for Root A's `-a`. Root D touches the SHARED declaration-arg construction (declare/export/local/readonly) — Task 3's no-regression sweep (esp. declare/export) is load-bearing. Confirm all category diffs via `diff .right <huck>` (inverted raw diff).
