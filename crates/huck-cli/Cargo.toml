[package]
name = "huck-cli"
version = "0.1.0"
edition = "2024"
description = "huck's interactive REPL + rustyline line-editor adapters"
license = "MIT"

[dependencies]
huck-engine = { path = "../huck-engine" }
rustyline = "18.0.0"
libc = "0.2"

[dev-dependencies]
expectrl = "0.9.0"
tempfile = "3"
